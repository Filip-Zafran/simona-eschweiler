"use client"; import {Announcement} from "@/lib/types";import {useLanguage} from "./LanguageProvider";import AnnouncementList from "./AnnouncementList";import PageHero from "./PageHero";
export default function UpdatesPage({items}:{items:Announcement[]}){const{locale}=useLanguage();return <main><PageHero eyebrow={locale==="de"?"Aktuelles":"Updates"} title={locale==="de"?"Hinweise & Neuigkeiten":"Notices & news"} text={locale==="de"?"Aktuelle Informationen zu Verfügbarkeit, Urlaub und Angeboten.":"Current information about availability, holidays and services."}/><section className="section news"><div className="container"><AnnouncementList items={items}/></div></section></main>}
